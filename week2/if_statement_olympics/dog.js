   /*
   //Preliminar using Ternay.
    // Exercise # 3
       
    var animalOne = "cat";
    var animalTwo = "dog";
    var compare;
    compare = (animalOne !== animalTwo) ? "No, is not the same" : "Yes, is the same";
    console.log(compare); // If the condition is true will show 3 is the length
  */

    //Using If/else statement....
    var animalOne = "cat";
    var animalTwo = "dog";
    var compare;
    if (animalOne !== animalTwo){
       console.log("No, is not the same");
    }
    else{
      console.log("Yes, is the same");
    }
