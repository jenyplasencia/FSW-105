// THIS IS THE TENARY CODE
/* var person = { 
    name: "Bobby", 
    age: 12,
};    

// Is allowed into the movies if person is 18 or older.
var compare = (person.age >= 18) ? "is allowed to go to the movie": "is not old enough to go to the movie";
console.log(person.name + " " + compare);
*/

//Using If statement.....
                             
 var person = {
        name: "Bobby",
        age: 12,
        };
if (person.age >= 18){
console.log((person.name) + ' ' + "is allowed to go to the movie");
}
else 
{ 
    console.log((person.name) + ' ' + "not old enought to go to the movies")
};
                            
                            