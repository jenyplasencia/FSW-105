       //Preliminar using Ternay.
    // Exercise # 2
   /*
    
    var animal = "cat";
    var compare;
    var countCaracters = animal.length;
    compare = (countCaracters === 3 && animal === "cat") ? "3 is the length" : "It is not a cat";
    
    console.log(compare); // If the condition is true will show 3 is the length
  
    */
   
    // Using If statement....
   var animal = "cat";
   var compare;
   var countCaracters = animal.length;
   if (countCaracters === 3 && animal === "cat"){
      
   };  console.log(" 3 is the lenght");
